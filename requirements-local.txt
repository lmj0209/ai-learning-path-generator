# ========================================
# 本地测试精简依赖（仅核心功能）
# 安装: pip install -r requirements-local.txt
# ========================================

# --- 必须的核心依赖 ---
python-dotenv==1.0.1
Flask>=2.0.1
pydantic==1.10.18
email-validator==2.1.0.post1
openai>=1.0.0
tiktoken>=0.5.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Migrate==4.0.7
Jinja2>=3.0.1
werkzeug>=2.0.1
requests>=2.28.0

# LangChain (轻量, 生成学习路径需要)
langchain==0.0.267
langsmith<0.1.0,>=0.0.21

# Google OAuth (可选, 不影响核心功能)
Flask-Dance[google]==7.1.0
google-auth>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=0.4.6

# --- 不需要的重型依赖（已移除）---
# torch           (~2GB, ML推理用)
# sentence-transformers (~1GB, 嵌入模型)
# chromadb        (向量数据库, 本地用SQLite替代)
# faiss-cpu       (向量搜索, 本地不需要)
# unstructured    (文档处理, 很重)
# redis/rq        (缓存队列, 本地不需要)
# wandb           (实验追踪, 本地不需要)
# onnxruntime     (推理引擎)
# transformers    (大模型, 本地不需要)
# scikit-learn    (ML库, 本地不需要)
# numpy/pandas    (被上面依赖自动安装)
